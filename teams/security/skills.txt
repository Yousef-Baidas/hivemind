# <owner/repo> <skill-name>   one per line; `bash teams/link-skills.sh` links them.
# Claude Code's built-in /security-review is always available; these add stack-specific checks.
# hive-scout rewrites this file for your stack during bootstrap; human approves the list.
github/awesome-copilot sql-code-review
better-auth/skills two-factor-authentication-best-practices
